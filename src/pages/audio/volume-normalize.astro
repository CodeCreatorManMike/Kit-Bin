---
import Layout from '../../layouts/Layout.astro';
import ToolSchema from '../../components/ToolSchema.astro';
import ToolWidget from '../../components/ToolWidget.astro';
import AdUnit from '../../components/AdUnit.astro';
---

<Layout
  title="Normalize Audio Volume Online — Free & No Upload Required | Kit-Bin"
  description="Normalize an audio file's volume to a consistent peak level. Runs entirely in your browser — your file is never uploaded to a server."
>
  <ToolSchema
    name="Normalize Audio Volume"
    description="Normalize an audio file's volume to a consistent peak level. Runs entirely in your browser — your file is never uploaded to a server."
    faqs={[
    {
      "q": "What format is the output?",
      "a": "WAV, regardless of the input format."
    }
  ]}
  />

  <div class="max-w-2xl mx-auto px-4 pt-10 pb-4 text-center">
    <h1 class="text-3xl font-bold text-slate-900 dark:text-slate-100">Normalize Audio Volume — Free</h1>
    <p class="mt-2 text-slate-600 dark:text-slate-400"><a href="/guides/how-kit-bin-works" class="hover:underline decoration-dotted underline-offset-2">Processed entirely in your browser. Your file is never uploaded to a server.</a></p>
  </div>

  <ToolWidget accept="audio/*" dropLabel="Drop an audio file here or click to browse" />

  <AdUnit />

  <div class="max-w-2xl mx-auto px-4 py-12 text-slate-700 dark:text-slate-300 space-y-4">
    <p>This tool boosts or reduces an audio file's volume so its loudest point sits just below clipping.</p>
    <p>Drop your audio file above. The normalized file downloads automatically once ready.</p>
    <h2 class="font-semibold text-lg pt-2">FAQ</h2>
    <div>
      <p class="font-medium">What format is the output?</p>
      <p class="text-slate-600 dark:text-slate-400">WAV, regardless of the input format.</p>
    </div>
    <p class="text-sm">Wondering what "normalize" actually changes? <a href="/guides/why-audio-sounds-quiet" class="text-blue-600 dark:text-blue-400 hover:underline">Read the guide</a></p>
  </div>

  <script>
    import { normalizeVolume } from '../../lib/audio/normalize';
    import { getToolElements, wireTool } from '../../lib/ui';

    const els = getToolElements();
    wireTool(els, {
      accept: 'audio/*',
      validate: (files) => (!files[0]?.type.startsWith('audio/') ? 'This tool needs an audio file.' : null),
      run: async (files) => {
        const blob = await normalizeVolume(files[0]);
        return { blob, filename: files[0].name.replace(/\.[^.]+$/, '-normalized.wav') };
      },
    });
  </script>
</Layout>
